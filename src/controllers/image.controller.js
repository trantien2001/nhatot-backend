const {
  getAllImage,
  getImageMotel,
  getAllImageActive,
} = require('../models/image.model');

const imageController = {
  getAllImage,
  getImageMotel,
  getAllImageActive,
};

module.exports = imageController;
